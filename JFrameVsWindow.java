import javax.swing.Jframe;
import javax.swing.JWindow;


public class JFrameVsWindow {
    JFrame f=new JFrame("Esto es un Jframe");
    f.setSize (350, 300); 
    f.setLocation (100, 100);
    f.setVisible (true):
    JWindow w= newJWindow();
    w.serSize (350, 300);
    w.setLocation (500, 100);
    w.getVisible (true);
    
}
